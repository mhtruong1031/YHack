websockets>=12.0
# Used by gpio_hp_test.py and other GPIO experiments (not by motors.py — sorts use servo_test*.py + ServoKit)
RPi.GPIO>=0.7.1
# servo_test3.py / 4 / 5 (execute_sort runs these via subprocess)
adafruit-circuitpython-servokit>=1.3.0
